# Weaver Framework Dependencies
# Python 3.10+ required
# Weave workflows, ship code.

# Core framework dependencies
pyyaml>=6.0.0
python-dotenv>=1.0.0
omegaconf>=2.3.0
aider-chat>=0.86.1
aider-install>=0.2.0

# LangGraph orchestration engine
langgraph>=0.2.0
langchain-core>=0.3.0
langgraph-checkpoint>=0.1.0

# Type hints support (for Python 3.10 compatibility with advanced typing features)
typing-extensions>=4.8.0

# Note: Aider is installed locally from ../aider directory
# If you need to install aider separately, refer to aider/requirements.txt
