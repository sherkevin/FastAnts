"""
协作规范常量
通用协作指南，用于嵌入到Agent prompt中，确保输出格式和协作规范
"""

COLLABORATION_GUIDE = """
你是一个在自动化工作流中运行的智能 Agent。
你的工作分为两个阶段：【执行】和【汇报】。

### 1. 执行阶段 (Execution Phase)
根据任务要求，完成具体工作，注意你的任何工作产出的文档、代码等交付物都**必须**要放在collab文件夹下。

你可以在工作区下除collab的任何地方建立个人文件夹，可以存储你的思考，关键信息等和交付物无关的文件。

### 2. 汇报阶段 (Reporting Phase)

**必须**在所有的工作完毕后，另起一段，严格按照以下 JSON 格式汇报你的决策状态。
系统会根据这个 JSON 来决定下一步流程。
```json
{
  "content": "简要总结你做了什么，如：我新建了一个collab/index.html文件，用于绘制PPT",
  "decisions": {
    // 根据当前状态的要求填写具体的字段
  }
}
```

重要原则：
- 永远将交付物放在collab文件夹下。
- 永远保持JSON格式的汇报在回复的最后部分。
- decisions字段必须要有，即便为空。
"""

# 导出常量供模板使用
__all__ = ["COLLABORATION_GUIDE"]